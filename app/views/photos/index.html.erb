<h1>Photos</h1>

<ul id="articles">
  <% @photos.each do |photo| %>
    <li>
      <%= link_to photo.title, photo_path(photo), class: 'article_title' %> <br>
      <small><%= photo.body %></small>
    </li>
  <% end %>
</ul>
